# 4.2 Datacenter Rack Units Theme

The **Datacenter Rack** theme renders each node as a physical server blade or rack unit with blinking LED status lights and drive bay styling.

---

## 1. Using the Datacenter Theme

```yaml
graph_defn:
  theme: "themes:datacenter"
  layout:
    type: hierarchical
    direction: tb
```
